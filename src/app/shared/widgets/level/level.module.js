import {LevelComponent} from './level.component';

export default angular
    .module('app.widgets.level', [])
    .component('level', LevelComponent())
     //.controller('LevelController', LevelController)
    .name;
